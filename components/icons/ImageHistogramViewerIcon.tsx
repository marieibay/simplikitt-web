import React, { SVGProps } from 'react';
import { BarChart } from 'lucide-react';

export const ImageHistogramViewerIcon: React.FC<SVGProps<SVGSVGElement>> = (props) => (
  <BarChart {...props} />
);
