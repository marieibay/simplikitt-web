import React, { SVGProps } from 'react';
import { FileText } from 'lucide-react';

export const ImageHeaderInfoViewerIcon: React.FC<SVGProps<SVGSVGElement>> = (props) => (
  <FileText {...props} />
);
