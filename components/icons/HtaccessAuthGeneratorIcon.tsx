import React, { SVGProps } from 'react';
import { FileLock } from 'lucide-react';

export const HtaccessAuthGeneratorIcon: React.FC<SVGProps<SVGSVGElement>> = (props) => (
  <FileLock {...props} />
);
