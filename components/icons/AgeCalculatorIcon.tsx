import React, { SVGProps } from 'react';
import { PersonStanding } from 'lucide-react';

export const AgeCalculatorIcon: React.FC<SVGProps<SVGSVGElement>> = (props) => (
  <PersonStanding {...props} />
);
