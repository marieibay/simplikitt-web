import React, { SVGProps } from 'react';
import { Replace } from 'lucide-react';

export const PlainTextToHtmlConverterIcon: React.FC<SVGProps<SVGSVGElement>> = (props) => (
  <Replace {...props} />
);
