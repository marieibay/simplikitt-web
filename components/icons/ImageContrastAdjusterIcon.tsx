import React, { SVGProps } from 'react';
import { Droplets } from 'lucide-react';

export const ImageContrastAdjusterIcon: React.FC<SVGProps<SVGSVGElement>> = (props) => (
  <Droplets {...props} />
);
