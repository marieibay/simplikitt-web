import React, { SVGProps } from 'react';
import { FileArchive } from 'lucide-react';

export const PdfFileSizeCheckerIcon: React.FC<SVGProps<SVGSVGElement>> = (props) => (
  <FileArchive {...props} />
);
