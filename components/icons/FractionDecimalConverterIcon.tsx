import React, { SVGProps } from 'react';
import { Columns } from 'lucide-react';

export const FractionDecimalConverterIcon: React.FC<SVGProps<SVGSVGElement>> = (props) => (
  <Columns {...props} />
);
