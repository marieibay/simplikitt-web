import React, { SVGProps } from 'react';
import { Droplets } from 'lucide-react';

export const ColorContrastCheckerIcon: React.FC<SVGProps<SVGSVGElement>> = (props) => (
  <Droplets {...props} />
);
