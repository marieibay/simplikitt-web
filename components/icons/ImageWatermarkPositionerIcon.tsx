import React, { SVGProps } from 'react';
import { Droplets } from 'lucide-react';

export const ImageWatermarkPositionerIcon: React.FC<SVGProps<SVGSVGElement>> = (props) => (
  <Droplets {...props} />
);
