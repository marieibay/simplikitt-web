import React, { SVGProps } from 'react';
import { Sparkles } from 'lucide-react';

export const ImageFilterPresetLibraryIcon: React.FC<SVGProps<SVGSVGElement>> = (props) => (
  <Sparkles {...props} />
);
