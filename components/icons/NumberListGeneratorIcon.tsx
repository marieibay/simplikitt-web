import React, { SVGProps } from 'react';
import { ListOrdered } from 'lucide-react';

export const NumberListGeneratorIcon: React.FC<SVGProps<SVGSVGElement>> = (props) => (
  <ListOrdered {...props} />
);
