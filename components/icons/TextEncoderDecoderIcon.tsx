import React, { SVGProps } from 'react';
import { Languages } from 'lucide-react';

export const TextEncoderDecoderIcon: React.FC<SVGProps<SVGSVGElement>> = (props) => (
  <Languages {...props} />
);
