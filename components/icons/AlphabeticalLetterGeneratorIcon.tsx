import React, { SVGProps } from 'react';
import { ListOrdered } from 'lucide-react';

export const AlphabeticalLetterGeneratorIcon: React.FC<SVGProps<SVGSVGElement>> = (props) => (
  <ListOrdered {...props} />
);
