import React, { SVGProps } from 'react';
import { CheckSquare } from 'lucide-react';

export const ImageThresholdFilterIcon: React.FC<SVGProps<SVGSVGElement>> = (props) => (
  <CheckSquare {...props} />
);
