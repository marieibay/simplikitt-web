import React, { SVGProps } from 'react';
import { HandCoins } from 'lucide-react';

export const SalesTaxCalculatorIcon: React.FC<SVGProps<SVGSVGElement>> = (props) => (
  <HandCoins {...props} />
);
