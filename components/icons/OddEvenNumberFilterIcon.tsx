import React, { SVGProps } from 'react';
import { ListChecks } from 'lucide-react';

export const OddEvenNumberFilterIcon: React.FC<SVGProps<SVGSVGElement>> = (props) => (
  <ListChecks {...props} />
);
