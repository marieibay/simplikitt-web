import React, { SVGProps } from 'react';
import { CheckSquare } from 'lucide-react';

export const ImageSharpenFilterIcon: React.FC<SVGProps<SVGSVGElement>> = (props) => (
  <CheckSquare {...props} />
);
