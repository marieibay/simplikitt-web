import React, { SVGProps } from 'react';
import { RemoveFormatting } from 'lucide-react';

export const RemoveExtraSpacesIcon: React.FC<SVGProps<SVGSVGElement>> = (props) => (
  <RemoveFormatting {...props} />
);
