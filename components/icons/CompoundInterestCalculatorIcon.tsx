import React, { SVGProps } from 'react';
import { Calculator } from 'lucide-react';

export const CompoundInterestCalculatorIcon: React.FC<SVGProps<SVGSVGElement>> = (props) => (
  <Calculator {...props} />
);
