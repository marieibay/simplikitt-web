import React, { SVGProps } from 'react';
import { CalendarClock } from 'lucide-react';

export const TimeAdderSubtracterIcon: React.FC<SVGProps<SVGSVGElement>> = (props) => (
  <CalendarClock {...props} />
);
