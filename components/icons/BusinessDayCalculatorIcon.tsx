import React, { SVGProps } from 'react';
import { Briefcase } from 'lucide-react';

export const BusinessDayCalculatorIcon: React.FC<SVGProps<SVGSVGElement>> = (props) => (
  <Briefcase {...props} />
);
