import React, { SVGProps } from 'react';
import { Binary } from 'lucide-react';

export const DecimalToBinaryConverterIcon: React.FC<SVGProps<SVGSVGElement>> = (props) => (
  <Binary {...props} />
);
