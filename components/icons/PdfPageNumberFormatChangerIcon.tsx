import React, { SVGProps } from 'react';
import { ListOrdered } from 'lucide-react';

export const PdfPageNumberFormatChangerIcon: React.FC<SVGProps<SVGSVGElement>> = (props) => (
  <ListOrdered {...props} />
);
