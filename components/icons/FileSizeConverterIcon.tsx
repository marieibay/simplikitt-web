import React, { SVGProps } from 'react';
import { FileArchive } from 'lucide-react';

export const FileSizeConverterIcon: React.FC<SVGProps<SVGSVGElement>> = (props) => (
  <FileArchive {...props} />
);
