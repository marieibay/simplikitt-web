import React, { SVGProps } from 'react';
import { Calculator } from 'lucide-react';

export const HexadecimalCalculatorIcon: React.FC<SVGProps<SVGSVGElement>> = (props) => (
  <Calculator {...props} />
);
