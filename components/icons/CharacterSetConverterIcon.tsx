import React, { SVGProps } from 'react';
import { Languages } from 'lucide-react';

export const CharacterSetConverterIcon: React.FC<SVGProps<SVGSVGElement>> = (props) => (
  <Languages {...props} />
);
