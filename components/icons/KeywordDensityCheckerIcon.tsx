import React, { SVGProps } from 'react';
import { ListChecks } from 'lucide-react';

export const KeywordDensityCheckerIcon: React.FC<SVGProps<SVGSVGElement>> = (props) => (
  <ListChecks {...props} />
);
