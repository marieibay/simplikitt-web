import React, { SVGProps } from 'react';
import { CheckSquare } from 'lucide-react';

export const PngTransparencyCheckerIcon: React.FC<SVGProps<SVGSVGElement>> = (props) => (
  <CheckSquare {...props} />
);
