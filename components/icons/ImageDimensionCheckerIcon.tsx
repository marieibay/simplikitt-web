import React, { SVGProps } from 'react';
import { Maximize } from 'lucide-react';

export const ImageDimensionCheckerIcon: React.FC<SVGProps<SVGSVGElement>> = (props) => (
  <Maximize {...props} />
);
