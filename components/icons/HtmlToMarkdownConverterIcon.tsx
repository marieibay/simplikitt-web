import React, { SVGProps } from 'react';
import { Replace } from 'lucide-react';

export const HtmlToMarkdownConverterIcon: React.FC<SVGProps<SVGSVGElement>> = (props) => (
  <Replace {...props} />
);
