import React, { SVGProps } from 'react';
import { FileScan } from 'lucide-react';

export const ImageOptimizationCheckerIcon: React.FC<SVGProps<SVGSVGElement>> = (props) => (
  <FileScan {...props} />
);
