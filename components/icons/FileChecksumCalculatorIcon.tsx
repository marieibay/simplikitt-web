import React, { SVGProps } from 'react';
import { FileCheck } from 'lucide-react';

export const FileChecksumCalculatorIcon: React.FC<SVGProps<SVGSVGElement>> = (props) => (
  <FileCheck {...props} />
);
